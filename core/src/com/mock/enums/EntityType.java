package com.mock.enums;

public enum EntityType {
	PLAYER,				// The player as controlled by the user
	
	STATIC_ENEMY,		// A fixed position enemy
	DYNAMIC_ENEMY,		// An enemy that changes position
	
	MOVING_PLATFORM,	// A platform which moves in some set pattern
	
	TEXT_ACTION,		// User can use action button to trigger information text
	
}
